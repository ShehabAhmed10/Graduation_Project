name: yemen_tourism_app
description: Yemen Tourism mobile app (Flutter)
publish_to: 'none'
version: 0.0.1+1
environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  dio: ^5.0.0
  flutter_secure_storage: ^8.0.0
  url_launcher: ^6.1.10
  flutter_map: ^4.0.0
  latlong2: ^0.8.1
 
flutter:
  uses-material-design: true
  assets:
    - assets/images/
